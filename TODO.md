# TODO

- [x] Fix malformed HTML in `index.html` (filters toolbar + validate structure)
- [x] Repair truncated/duplicated/malformed CSS in `index.css` (label/button/app rules)
- [ ] Reformat/cleanup `index.js` for readability (keep behavior)
- [ ] Smoke-test by opening `index.html` and verifying: add/edit/delete, filters, search, counts

